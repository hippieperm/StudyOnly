import 'package:flutter/material.dart';

class V22HomeScreen extends StatelessWidget {
  const V22HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Color(0xff212528),
    );
  }
}
